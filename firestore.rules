rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // السماح الكامل لجميع العمليات لتبسيط التطوير وتجاوز مشاكل الصلاحيات
    match /{document=**} {
      allow read, write: if true;
    }
  }
}