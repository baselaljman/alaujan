rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // السماح الكامل لجميع العمليات على كافة المسارات والمجموعات
    // تم التبسيط لأقصى درجة لحل مشاكل الصلاحيات في مرحلة التطوير
    match /{document=**} {
      allow read, write: if true;
    }
  }
}