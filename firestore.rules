rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // السماح بالقراءة والكتابة العامة لجميع الجداول بدون قيود تقنية لضمان سلاسة العمل
    match /{document=**} {
      allow read, write: if true;
    }
  }
}