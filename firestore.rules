
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // قاعدة عامة تسمح بالقراءة والكتابة للجميع لسهولة التطوير والاختبار
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
