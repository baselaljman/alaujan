rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // فتح القواعد بالكامل لضمان سلاسة العمل للجميع بناءً على طلب العميل
    match /{document=**} {
      allow read, write: if true;
    }
  }
}